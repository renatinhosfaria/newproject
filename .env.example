APP_PORT=8080
APP_ORIGIN=http://127.0.0.1:8080
DB_OWNER_PASSWORD=
DB_APP_PASSWORD=
SEED_SUPERVISOR_EMAIL=
SEED_SUPERVISOR_PASSWORD=
SEED_BROKER_EMAIL=
SEED_BROKER_PASSWORD=
# Use URL-safe hex values for database passwords: openssl rand -hex 32
# Generate each seed password locally: openssl rand -hex 32
